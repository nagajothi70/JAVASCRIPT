let arr=[1,2,3,4,5]
    for(let fruits of arr){  // simple way of loops
        console.log(fruits)
    }


    // for in is used to handle objects
    let hi={
        name:"jothi",
        class:"it",
        year:"iii"
    }
    for(let bii in hi){
        console.log(bii)
        console.log(hi[bii])
    }
