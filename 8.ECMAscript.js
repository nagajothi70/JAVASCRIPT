let fname="naga";
        let lname="jothi";
        let city="erode";
        console.log(fname + " " +lname +" "+"lives in "+city);
    // see the above example for simple statement we have used more than 5 + symbols
     // to replace this use ecma scripts-----``--backtick
     let ecmascript=`${fname} ${lname} lives in ${city}`
     console.log(ecmascript)
     console.log(`${fname} ${lname}`);
     // both gives same output 
     let str= `happy
birthday`
     console.log(str)
     let str1=`"hi she is best girls her's fater has expensive bike"`
     console.log(str1)
